% Ejercicio 1
% a

QUERY FORALL (x, y, z: INT): x <= y AND y <= z => x <= z ;

% b

QUERY FORALL (a, b: INT): a < b OR a = b OR a > b ;

% c

QUERY FORALL (a, b: INT): a <= b AND b <= a+1 => b = a OR b = a + 1 ;
